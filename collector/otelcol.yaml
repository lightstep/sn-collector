receivers:
  azureeventhub:
    connection: Endpoint=sb://lsintegrations.servicebus.windows.net/;SharedAccessKeyName=listen-only;SharedAccessKey=qzE12+MVX80BQYioPtDOFGNT4+jNk4tFA+AEhIHtwe8=;EntityPath=mainhub

exporters:
  logging:
    loglevel: debug
  otlp:
    endpoint: ingest.lightstep.com:443
    headers:
        "lightstep-access-token": "${LS_ACCESS_TOKEN}"

processors:
  batch:

service:
  telemetry:
    logs:
  pipelines:
    logs:
      receivers: [azureeventhub]
      processors: [batch]
      exporters: [otlp]
